{
  // See https://go.microsoft.com/fwlink/?LinkId=733558
  // for the documentation about the tasks.json format
  "version": "2.0.0",
  "tasks": [
    {
      "label": "build nassau",
      "type": "shell",
      // "command": "g++ -g -DEVAL -std=c++11 -O2 -pipe -static -s -o nassau nassau.cpp",
      "command": "g++",
      "args": [
        "-g",
        "-o",
        "nassau",
        "nassau.cpp"
      ],
      "group": {
        "kind": "build",
        "isDefault": true
      }
    },
  ]
}